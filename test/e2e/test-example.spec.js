describe('integration test example', function(){

	it('should run in appveyor', function(){
		expect(1).toBe(2);
	});

	it('should run in appveyor and be valid', function(){
		expect(1).toBe(1);
	});
	
});